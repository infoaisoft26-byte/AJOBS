import { CompanyProfile } from "./EmployerTypes";

export async function seedEmployerDataIfEmpty(_userId: string, _userName: string) {
  // Production mode: No automatic demo dataset seeding.
  return;
}
